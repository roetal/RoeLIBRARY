<Page x:Class="Library.AddAuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Library"
      Title="Add Author Page">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Add Author" FontSize="24" FontWeight="Bold" Margin="10"/>

        <!-- Content -->
        <Border Grid.Row="1" Margin="10"  BorderThickness="1" Padding="10">
            <StackPanel>
                <TextBlock Text="Author Name:"/>
                <TextBox x:Name="AuthorNameTextBox" Margin="0 5 0 10"/>

                <TextBlock Text="Author Bio:"/>
                <TextBox x:Name="AuthorBioTextBox" Height="100" AcceptsReturn="True" TextWrapping="Wrap" Margin="0 5 0 10"/>

                <Button Content="Add Author" HorizontalAlignment="Center" Width="120" Click="AddAuthorButton_Click" Margin="0 10"/>
            </StackPanel>
        </Border>

    </Grid>
</Page>
