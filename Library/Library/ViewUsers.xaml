<Page x:Class="Library.ViewUsers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Library"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="ViewUsers">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Username" FontWeight="Bold" FontSize="16" Margin="10" Grid.Row="0" Grid.Column="0"/>
        <TextBlock Text="User Details" FontWeight="Bold" FontSize="16" Margin="10" Grid.Row="0" Grid.Column="1"/>

        <ScrollViewer Grid.Row="1" Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="10">
            <ListBox x:Name="userListBox" SelectionChanged="UserListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Username}" Width="150"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10" VerticalAlignment="Top">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Margin" Value="0,0,0,5"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </StackPanel.Resources>

            <TextBlock Text="Username:"/>
            <TextBox x:Name="usernameTextBox" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Email:"/>
            <TextBox x:Name="emailTextBox" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Phone:"/>
            <TextBox x:Name="phoneTextBox" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Password:"/>
            <TextBox x:Name="passwordTextBox" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Is Admin:"/>
            <CheckBox x:Name="isAdminCheckBox" Margin="0,0,0,10" Checked="IsAdminCheckBox_Checked" Unchecked="IsAdminCheckBox_Unchecked"/>

            <Button Content="Delete User" Click="DeleteUserButton_Click" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
